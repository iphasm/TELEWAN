# Archivo de configuración de variables de entorno para TELEWAN Bot
# Copia este archivo como .env y configura los valores necesarios

# ===== CONFIGURACIÓN OBLIGATORIA =====

# Token del bot de Telegram (obtenlo de @BotFather en Telegram)
# CRÍTICO: Sin esto el bot no puede funcionar
TELEGRAM_BOT_TOKEN=tu_token_de_telegram_aqui

# API Key de Wavespeed (obténla de https://wavespeed.ai)
# CRÍTICO: Sin esto no se pueden generar videos
WAVESPEED_API_KEY=tu_api_key_de_wavespeed_aqui

# ===== CONFIGURACIÓN OPCIONAL =====

# ID de usuario autorizado (opcional - si no se configura, permite a todos)
# Útil para bots privados
ALLOWED_USER_ID=tu_user_id_aqui

# URL base de la API de Wavespeed (opcional, valor por defecto ya configurado)
WAVESPEED_BASE_URL=https://api.wavespeed.ai

# Modelo por defecto ('ultra_fast', 'fast', 'quality', 'text_to_video')
DEFAULT_MODEL=ultra_fast

# Prompt por defecto cuando no se proporciona caption
# Este es el prompt que se usa automáticamente cuando envías una foto sin texto
DEFAULT_PROMPT=Absolutely fixed face and head position, zero head movement. No camera movement — a static, hyper-detailed cinematic shot. She swiftly reaches and removes her entire dress with decisive and strong motion, revealing bare natural breasts with visible nipples, fully naked body without underwear beneath exposing genitals. Skin texture rendered with extreme realism — soft, lifelike, with natural pores and subtle sheen under soft, diffused lighting. Moody, atmospheric tone with low-key shadows and ambient glow, evoking intimacy and tension. Shot in 4K resolution with shallow depth of field, blurring the background to isolate the subject. Cinematic composition, film grain subtly present, emphasizing emotional gravity and visual precision.

# Negative prompt automática (filtros para mejorar calidad)
# Elementos que se excluyen automáticamente de todas las generaciones
NEGATIVE_PROMPT=low quality, worst quality, blurry, artifacts, distortion, deformed, disfigured, ugly, extra limbs, mutated hands, malformed, poor anatomy, distorted face, distorted features, melting face, face morphing, changing face, changing identity, different person, text, watermark, logo, censored, mosaic, black bars, static camera, looped motion, bad transitions, fade transitions, jitter, flicker, clothing, underwear, bra, panties, shirt, pants, accessories, watch, smartwatch, cartoon, 3d render, doll, plastic skin, overexposed, underexposed, cluttered background

# ===== CONFIGURACIÓN DE WEBHOOKS (solo para Railway/Heroku) =====

# Activar modo webhook (true/false)
USE_WEBHOOK=false

# URL completa del webhook (proporcionada por Railway/Heroku)
WEBHOOK_URL=https://tu-app.railway.app

# Puerto del webhook (Railway asigna automáticamente la variable PORT)
# No cambies esto a menos que sepas lo que haces
WEBHOOK_PORT=8443

# Path del webhook
WEBHOOK_PATH=/webhook

# Token secreto para validar webhooks (opcional pero recomendado)
WEBHOOK_SECRET_TOKEN=tu_token_secreto_aqui

# ===== CONFIGURACIÓN DE ALMACENAMIENTO =====

# Directorio para almacenar archivos temporales
VOLUME_PATH=./storage

# ===== INSTRUCCIONES =====
# 1. Copia este archivo como .env: cp env_example.txt .env
# 2. Configura TELEGRAM_BOT_TOKEN y WAVESPEED_API_KEY
# 3. Las demás variables son opcionales y tienen valores por defecto
# 4. Para webhooks, configura USE_WEBHOOK=true y WEBHOOK_URL

